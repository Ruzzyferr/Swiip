# Derin inceleme — 2026-08-20

`docs/derin-inceleme-promptu.md` uyarınca yapılan inceleme. Her bulgu için **kanıt**,
**sebep**, **düzeltme** ve **hangi test bunu bir daha bırakmaz** yazılı.

Başlangıç durumu: 1.649 test yeşil, `npm run verify` temiz.
Bitiş durumu: **1.701 test yeşil**, `npm run verify` temiz.

İnceleme üç turda yapıldı. İlk tur A (kod), B (site) ve G (yetki) bölümlerini kapattı.
İkinci tur emülatörde C (32 ekran), E (14 persona), F (rakip farkı) ve D (MCP) bölümlerini
kapattı ve sekiz kusur daha çıkardı. Üçüncü tur kamerayı ve bildirimleri cihazda çalıştırdı
ve **beş kusur daha** çıkardı.

**Yirmi üç bulgunun on üçü yalnızca cihazda görülebilirdi.** Bu incelemenin en pahalı
dersi bu: kod okuyarak bulunamayan bir kusur sınıfı var ve o sınıf ürünün en somut
vaatlerine dokunuyor — ücretsiz katmanın tek çıktısı, tıbbi feragat, gizlilik cümlesi.

Yöntem notu: her düzeltme önce kırmızı testle gösterildi ve **her koruma bilerek
bozularak** kırmızıya döndüğü doğrulandı. Bozunca kırmızıya dönmeyen test, test değildir.

---

## Özet

| # | Bulgu | Ağırlık | Durum |
|---|---|---|---|
| 1 | İlerleme döngüsü açıktı: motor karar veriyor, plan hiç değişmiyordu | **Kritik** | Düzeltildi |
| 2 | Kaydırmalı öğün değiştirme hiçbir şeyi değiştirmiyordu | **Yüksek** | Düzeltildi |
| 3 | Yetki açığı: başkasının programındaki hareket değiştirilebiliyordu | **Yüksek** | Düzeltildi |
| 4 | Akşam öğünü sessizce öğle hedefine düşüyordu | **Yüksek** | Düzeltildi |
| 5 | Dolap kaydedilemediğinde "kaydedildi" yazıyordu | Orta | Düzeltildi |
| 6 | Sitede geniş ekranda tipografi değil boşluk büyüyordu | Orta | Düzeltildi |
| 7 | Dil notu, kendisi için geçerli olmayan kullanıcıya gösteriliyordu | Düşük | Düzeltildi |
| 8 | Çevrimdışı program yaşını söylemiyordu | Düşük | Düzeltildi |
| 9 | Deload set kuralının ikinci kopyası + tek set kenar durumu | Düşük | Düzeltildi |
| 10 | "Neon ve turuncu yok" kuralını hiçbir şey denetlemiyordu | Süreç | Denetim eklendi |
| 11 | Bozuk istek 500 dönüyordu: istemcinin hatası sunucu hatası gibi görünüyordu | **Yüksek** | Düzeltildi |
| 12 | Koyu temada **her ana düğme** okunmuyordu (2.86:1) | **Yüksek** | Düzeltildi |
| 13 | İngilizce arayüzde aynı hareket iki ayrı adla görünüyordu | Orta | Düzeltildi |
| 14 | İngilizce arayüzde haftalık planda "KAHVALTI" yazıyordu | Orta | Düzeltildi |
| 15 | Deste mesajı motorda Türkçe sabitlenmişti | Orta | Düzeltildi |
| 16 | İlerleme ekranı hareket adı yerine ham katalog kimliği basıyordu | Orta | Düzeltildi |
| 17 | Hesap silme yakalanmamış promise reddi bırakıyordu | Orta | Düzeltildi |
| 18 | Bildirim ekranı çevrimdışıyken sebebi söylemiyordu | Düşük | Düzeltildi |
| 19 | **Ücretsiz kullanıcı kendi vücut analizini hiç göremiyordu** | **Kritik** | Düzeltildi |
| 20 | Okunan raporda tıbbi cihaz feragati Türkçe kalıyordu | **Yüksek** | Düzeltildi |
| 21 | Gizlilik notu fotoğraf gönderildiğini inkâr ediyordu | **Yüksek** | Düzeltildi |
| 22 | Uygulama açıkken bildirimler sessizce kayboluyordu | Orta | Düzeltildi |
| 23 | Beş hatırlatma türü tek bir yedek kanaldaydı | Orta | Düzeltildi |

Ayrıca **kendi ölçüm araçlarımda iki hata** bulundu; ikisi de sahte sonuç üretiyordu ve
düzeltilmeden önce yanlış bulgu raporlamama ramak kalmıştı (bkz. "Ölçüm araçları yanıldı").

---

## 1. İlerleme döngüsü açıktı — motor karar veriyor, plan hiç değişmiyordu

**Ağırlık: kritik.** `CLAUDE.md`'deki kilitli karar: *"Program statik doküman değil; her
seans önceki geri bildirimden hesaplanır."* Ürünün ana vaadi buydu ve tutmuyordu.

### Kanıt

`packages/api/src/rotalar/ilerlemeDongusu.test.ts`, düzeltmeden önce:

```
× "tamamladım" sonrası hareketin plandaki hedef ağırlığı artar
  → expected 20 to be greater than 20
× iki hafta üst üste zorlanınca plandaki set sayısı düşer
  → expected 4 to be 3
× hafta ilerletilebilir ve kazanılan yük sıfırlanmaz
  → expected 404 to be 200
```

Yani: motor kullanıcıya *"Bench 52,5 kg'a çıkıyor"* diyor, kullanıcı programı açıyor ve
hâlâ 50 kg yazıyor. Motor *"iki hafta üst üste zorlandın, hacmi bir set düşürdüm"* diyor,
set sayısı aynı kalıyor. Uygulama kullanıcıya **yapmadığı bir şeyi söylüyordu.**

### Sebep

Üç ayrı kopukluk, üçü de sessiz:

1. `progression_state` geri bildirimde güncelleniyordu ama onu okuyan tek yer bir sonraki
   geri bildirim hesabıydı. `/v1/program/aktif` seans kalemlerini olduğu gibi döndürüyor;
   `target_weight`, `target_reps_*` ve `target_sets` yalnızca `/uret` anında yazılıyordu.
2. `ilerlemeUygula` içindeki `set_degisimi` motorda hesaplanıp **hiçbir yerde
   okunmuyordu** (`git grep set_degisimi` → yalnızca kendi dosyası). Deload'un *"set
   sayısını düşürdüm"* cümlesi karşılıksızdı; deload haftası hafiflemiyorsa deload olmaz.
3. `/v1/program/uret` uygulamanın tamamında **tek yerden** ve `{hafta: 1}` sabitiyle
   çağrılıyordu (`apps/mobile/app/(sekme)/program.tsx:123`). Sonraki hafta diye bir şey
   yoktu: kullanıcı dört seansını bitiriyor, hepsi "TAMAM" oluyor, program orada
   donuyordu. *"İki hafta sonra dönen kullanıcı"* aynı haftayı görüyordu.

   Bunun sessiz bir yan etkisi vardı: `programs.hafta` hep 1 kaldığı için takvim tabanlı
   deload koşulu (`hafta - son_deload_hafta >= 4..6`) **hiç sağlanamıyordu.** Deload'un
   tek çalışan yolu üst üste zorlanma sinyaliydi.

Mevcut test bu boşluğu göremiyordu çünkü geri bildirim ucundan dönen **cümleye** bakıyordu
(`expect(kararlar[0]).toMatch(/artıyor|tekrara/)`), planın kendisine değil. Cümlenin doğru
olması, kullanıcının o sayıyı görmesi anlamına gelmiyor.

### Düzeltme

- `planaYansit()` — motor kararı programın **henüz yapılmamış** seanslarına yazılıyor.
  Tamamlanmış seans bir kayıttır; geriye dönük değiştirmek kullanıcının yaptığı işi
  değiştirmek olurdu.
- `POST /v1/program/sonraki-hafta` — hafta ilerletiliyor. Uç, en az bir seans
  işaretlenmeden çalışmıyor; yoksa peş peşe basmak haftayı 52'ye taşır ve kullanıcı hiç
  antrenman yapmadan "deload haftası" alırdı.
- Yeni hafta ilerlemeyi **taşıyor**: daha önce çalışılmış hareket kaldığı yerden devam
  ediyor, yeni hareketin başlangıç yükü ölçülen e1RM'den hesaplanıyor. Motorun güven
  çarpanı (yeni başlayanda 0,78) altı hafta gerçek veriden sonra yeniden uygulanırsa
  kullanıcı geri gönderilirdi — o çarpan ilk hafta doğru, altıncı hafta yanlış.
- Mobilde hafta bitince "Sonraki haftayı hesapla" kartı çıkıyor. Karar `shared`'da
  (`haftaBittiMi`), ekranda hesap yapılmıyor.

### Hangi test bir daha bırakmaz

`packages/api/src/rotalar/ilerlemeDongusu.test.ts` — cümleye değil **plana** bakıyor.
Doğrulama: `planaYansit` çağrısı silinince ilk iki test, ilerleme taşıma silinince üçüncü
test kırmızıya dönüyor (`expected 20 to be greater than or equal to 22` — kullanıcı 22
kg'dan 20 kg'a geri düşüyor).

---

## 2. Kaydırmalı öğün değiştirme hiçbir şeyi değiştirmiyordu

Spec bölüm 10 özelliğin adını *"kaydırmalı öğün **değiştirme**"* koyuyor; plan ekranındaki
düğmenin adı **"Değiştir"**.

### Kanıt

`meal_plans` tablosuna yazan tek yer `POST /ogun/plan` (tüm hafta). Plandaki tek bir öğünü
değiştiren uç **yoktu**. Sağa kaydırma yalnızca `POST /ogun/kaydirma` ile tercih
öğreniyor, sonra `router.back()` çağırıyordu: kullanıcı beğendiği tarifi seçiyor, plana
dönüyor ve **eski öğünü görüyordu.**

İkinci kanıt aynı sınıftan: `packages/shared/src/islem.ts` içinde `ogun_degistir`
("Öğün değiştirilemedi" / "The meal could not be swapped") mesajı iki dilde yazılıydı ve
**hiçbir yerden çağrılmıyordu.** Birisi değiştirmeyi tasarlamış, hata cümlesini yazmış,
bağlamamış.

Üçüncüsü: plan ekranındaki "Değiştir" düğmesi `/ogun/deste`'ye **parametresiz**
gidiyordu (`router.push('/ogun/deste')`). Deste `ogun` durumunu `'ogl'` sabitiyle
başlatıyordu — yani pazartesi kahvaltısına basan kullanıcıya öğle destesi açılıyordu.

### Düzeltme

- `POST /v1/ogun/degistir` — plandaki tek öğünü değiştirir ve alışveriş listesini yeniden
  üretir (yoksa kullanıcı markette artık pişirmeyeceği yemeğin malzemesini alır).
- Tarif **desteden** seçilmek zorunda: desteyi geçmeyen tarif sert kısıtları (alerji,
  dini/etik, intolerans, bütçe, süre) ve makro kilidini geçmemiş demektir. Kullanıcının
  göremediği bir tarifi plana yazmak kısıt çözücüsünü atlatmak olurdu.
- Plana dilden bağımsız `kod` yazılıyor; eski planlar için kaloriye göre yedek eşleşme var.
- Plan ekranı gün ve öğün kodunu geçiriyor; deste sağa kaydırmada gerçekten değiştiriyor
  ve hata olursa `ogun_degistir` cümlesini gösteriyor.

### Hangi test bir daha bırakmaz

`packages/api/src/rotalar/ogunDegistirme.test.ts` — "öğün değiştirilince plandaki tarif
gerçekten değişir" ve "diğer günler değişmemeli". `days_jsonb` yazması silinince kırmızı
(`expected 'dana-bonfile-izgara' to be 'atistirmalik-nohut'`).

---

## 3. Yetki açığı — başkasının programındaki hareket değiştirilebiliyordu

**Ağırlık: yüksek, ve asıl mesele sızıntı değil sağlık.**

### Kanıt

`packages/api/src/rotalar/kiraciAyrimi.test.ts`, düzeltmeden önce:

```
× B, A'nın programındaki hareketi değiştiremez
  → expected 200 to be greater than or equal to 400
```

### Sebep

`POST /v1/program/hareket-degistir`, `session_items`'ı **yalnızca `session_id`** ile
seçiyor, güncellemeyi de `session_items.id` ile yazıyordu. Sahiplik kontrolü yoktu.

Tehlikeli kısım şu satır:

```ts
const profil = await profiliGetir(istek.kullaniciId);
const zincir = muadilZinciri(govde.eski_hareket_id, {
  ekipman: profil.kisitlar.ekipman,
  kontrendikasyonlar: profil.kisitlar.kontrendikasyonlar,
});
```

Muadil zinciri **isteği yapanın** profilinden hesaplanıyor. Yani saldırganın ekipmanı ve
kontrendikasyonları, kurbanın programına yazılacak hareketi belirliyordu: bel fıtığı olan
bir kullanıcının programına yerden çekiş girebilirdi. Ürünün dört sert kapısından biri
böyle delinir.

Aynı dosyadaki `/geri-bildirim` ve `/seans/:id/atla` bu kontrolü **zaten yapıyordu.**
Tutarsızlığın kendisi de bulgu: kural yazılıydı, her yere uygulanmamıştı.

### Düzeltme

Seans, istek sahibine ait değilse 404. ("Bulunamadı", "yasak" değil — kaydın varlığını da
sızdırmamak için.)

### Hangi test bir daha bırakmaz

`kiraciAyrimi.test.ts` (8 test) bu sınıfı **genel** olarak koruyor: kimlik taşıyan her uç
için "B, A'nın kaydına dokunabiliyor mu" ayrı ayrı soruluyor — seans geri bildirimi, seans
atlama, hareket değiştirme (hem okuma hem yazma yolu), sonraki hafta, öğün değiştirme,
dolap, yemek kaydı silme ve dışa aktarma.

`erisim.test.ts` *"token var mı"* diye soruyordu; bu dosya *"token başkasının kaydına
yetiyor mu"* diye soruyor. İkisi farklı sorulardır.

---

## 4. Akşam öğünü sessizce öğle hedefine düşüyordu

### Kanıt

```
× akşam destesi akşam hedefini kullanır, öğleye düşmez
  → expected 930 to be greater than 930
```

Akşam destesi ile öğle destesi **birebir aynı** hedefi döndürüyordu.

### Sebep

`/deste` ucu öğünü `kod` ile değil Türkçe **adın** ilk üç harfiyle eşleştiriyordu:

```ts
ogunler.find((o) => o.ad.toLocaleLowerCase('tr-TR').startsWith(ogun.slice(0, 3)))
  ?? ogunler[1]!.hedef
```

`'Akşam'.toLocaleLowerCase('tr-TR')` → `'akşam'`, aranan önek `'aks'`. Türkçe `ş` ASCII
`s` değil, yani eşleşme **hiç** tutmuyor ve kod sessizce `ogunler[1]` yedeğine — yani
öğleye — düşüyordu. Dağılım: kahvaltı %25, öğle %35, akşam %40. Kullanıcı akşam için öğle
boyunda tarifler görüyordu; ürünün *"makro kilidi ±%8"* sözü tam burada kırılıyordu.

Ramazan'da daha kötüydü: kodlar `sahur` / `iftar` / `iftar_sonrasi` olduğu için **üç
öğünün üçü birden** yedeğe düşüyordu.

Bu, projenin `docs/derin-inceleme-promptu.md`'de kendi uyardığı Türkçe locale tuzağının
yeni bir örneği. `shared/arama.ts` içindeki `aramaAnahtari` tam bunun için var; burada
kullanılmamış, üstelik dilden bağımsız `kod` alanı zaten mevcutken.

### Düzeltme

`ogunCozumle()` — dilden bağımsız `kod` ile eşleşme. Tanınmayan kod sessizce başka bir
öğüne düşmek yerine 400 veriyor. Deste sekmeleri artık sunucudan geliyor, yani Ramazan'da
liste kendiliğinden sahur/iftar oluyor (istemci üç sabit sekme çiziyordu).

---

## 5. Dolap kaydedilemediğinde "kaydedildi" yazıyordu

`apps/mobile/app/ogun/dolap.tsx` hatayı yutup **koşulsuz** `setKaydedildi(true)`
çalıştırıyordu. Kullanıcı dolabını giriyor, yeşil "kaydedildi" notunu görüyor ve listeyi
kaybediyordu.

`islem.ts` bu durum için `dolap_kaydet` cümlesini iki dilde taşıyordu ve hiçbir yerden
çağrılmıyordu. Onbir yazma işleminden **üçünün** karşılığı ölüydü: `dolap_kaydet`,
`plan_uret`, `ogun_degistir`.

Ek olarak `alisveris.tsx`'te dolaba ekleme `void istek(...).then(...)` biçimindeydi:
istek patlarsa yakalanmamış bir promise reddi kalıyor, kullanıcıya hiçbir şey
söylenmiyor ve ekran da değişmiyordu. Kullanıcı dokunuyor, hiçbir şey olmuyor.

Düzeltme: başarısız kayıtta "kaydedildi" yazılmıyor, cümle gösteriliyor ve desteye
geçilmiyor (deste kaydedilmemiş dolaba göre açılırdı).

> `plan_uret` bilerek bağlanmadı: `plan.tsx` sunucunun kendi mesajını gösteriyor ve o
> mesaj daha özgül. Genel cümleye düşürmek bilgi kaybı olurdu.

---

## 6. Sitede geniş ekranda tipografi değil boşluk büyüyordu

CSS'in **kendi yorumu** şöyle diyor:

> `vw katsayısı bilerek yüksek — geniş ekranda boşluk değil, tipografi büyüyor.`

Kural yazılıydı, tutulmuyordu.

### Kanıt — `scripts/site-tipografi.mjs`, Motor bölümü

| Genişlik | Başlık | Gövde | Sütun | Sağda boşluk |
|---|---|---|---|---|
| 1280px | 45px | 22px | 542px | **%47** |
| 1920px | 56px | 24px | 564px | **%63** |
| 2560px | 56px | 24px | 564px | **%72** |
| 3440px | 56px | 24px | 564px | **%79** |

3440 pikselde metin **736px'te bitiyor**, görünüm 3425px. Kullanıcının kuralı: *"her
genişlikte sağda solda boşluk kalmayacak, sayfa PDF gibi ortaya dizilmiş görünmeyecek."*

### Sebep

1. `.bolum-bas { max-width: 46ch }` — satır uzunluğu için doğru, **blok** genişliği için
   yanlış. Etiket, başlık ve giriş metni üst üste dizilip dar bir sütuna hapsoluyordu.
2. `--h2` tavanı `3.5rem`'e **1900px'te**, `--lede` tavanı `1.5rem`'e **1600px'te**
   doluyordu. Yani 1900'den sonra büyüyen tek şey boşluktu. `--dev` ise 2640px'e kadar
   büyümeye devam ediyor — ölçek kendi içinde tutarsızdı.

### Düzeltme

80rem üstünde başlık bloğu iki sütuna açılıyor (başlık solda, giriş metni sağda,
tabanları hizalı) ve tavanlar 2560'ta dolacak şekilde yükseltildi. Satır uzunluğu
bozulmadı: her sütun kendi okunabilir ölçüsünü koruyor.

**Sonuç:** 3440'ta sağ boşluk %79 → **%19**; 1280–1920 arası %47–63 → **%3**. Başlık
56 → 72px, gövde 24 → 28px, satır uzunluğu 51–67 karakter (okunabilir aralıkta).

### Site — kusur bulunmayanlar

Bunlar ölçüldü ve temiz çıktı, kayda geçsin diye yazılıyor:

- **Kontrast:** üç sayfa × iki tema, her metin ögesi altında gerçekten boyanan renge
  karşı — hepsi eşiği geçiyor. Koyu temadaki eski Motor bandı kusuru (1.08:1) kapalı.
- **Taşma:** 14 genişlik × 2 tema × 6 bölüm = **168 ekran görüntüsü** alındı ve bakıldı.
  Yatay taşma yok, konsol hatası yok, kumpas ağzı altı bölümde de etiketin üstünde.
- **Klavye:** "İçeriğe atla" gerçek bir hedefe gidiyor; 14 odaklanabilir ögenin hepsinde
  görünür odak halkası var, görünmez odak yok.
- **`prefers-reduced-motion`:** açıkken hiçbir animasyon veya geçiş kalmıyor.
- **Marka rengi:** neon ve turuncu yok (aşağıya bakınız).

---

## 7-9. Tanımlı ama hiç çağrılmayanlar

337 çalışan export tarandı. Üç gerçek kusur, iki ölü kod:

- **`kendiVerisiMi`** yazılmıştı, hiçbir yerden çağrılmıyordu. Ayarlardaki dil notu
  *koşulsuz* gösteriliyordu: Türk kullanıcı *"hareket talimatları, tarifler ve vücut
  analizi raporu şimdilik yalnızca Türkçe"* cümlesini okuyordu. Onun için bu bir kısıt
  değil; sadece kafa karıştıran bir uyarı. Not artık yalnızca veri yedeğine düşen
  kullanıcıya gösteriliyor — fonksiyonun kendi yorumunun baştan beri söylediği şey buydu.
- **`okuYas`** yazılmıştı, hiçbir yerden çağrılmıyordu. Çevrimdışı notu *"bu, cihazında
  kayıtlı son programın"* diyordu ama **yaşını** söylemiyordu. İki hafta önce kaydedilmiş
  bir program bugünkü sanılabiliyordu. Not artık son güncelleme tarihini taşıyor.
- **`deloadSetSayisi`** ikinci bir kopya olarak duruyordu; kullanılan satır içi ifade tek
  setlik bir harekette **0 set** üretiyordu (`-max(1, round(1 × 0.4))` → `-1`).
  Adlandırılmış kural tabanda 1 tutuyor. Kural tek kaynağa indirildi.
- **`dildenVeriYereli`** silindi — `veriYereli`'nin bir satırlık takma adı, hiç
  çağrılmıyordu.
- **`sureBazliMi`** silindi — `hacimSayilir` aynı alanı zaten okuyor.

  Silmeden önce kontrol edildi: süre bazlı 22 hareketin dokuzu ısınma değil ve biri
  (`kurek-makinesi`) gerçek bir `cekme_yatay` paterni taşıyor. Programa girseydi "3 × 8-12
  tekrar kürek makinesi" yazardı. Girmiyor: `havuz.ts:37` bunları havuzdan eliyor. Yani
  burada kusur **yok**, fonksiyon yalnızca ikinci bir kopyaydı.

> Tarama `usersRelations` ve kardeşlerini de ölü gösterdi ama **değiller** — drizzle'a
> `schema` olarak veriliyorlar. `aylikMaliyetTahmini`, `PISIRME_VERIMI`,
> `toplamSoruSayisi` ve `cumleBasiHarfi` yalnızca testlerden çağrılıyor; bunlar sınanmış
> kamu yüzeyi, silinmedi.

---

## 10. "Neon ve turuncu yok" kuralını hiçbir şey denetlemiyordu

`CLAUDE.md`'deki kilitli karar bugüne kadar yalnızca yazılıydı. Yeni bir vurgu rengi
eklenmesi ya da bir kütüphane renginin sızması sessiz kalırdı.

`scripts/site-renk-denetimi.mjs` her ögenin gerçekten hesaplanan metin, arka plan ve
kenarlık rengini okuyup HSL'e çeviriyor; turuncu (ton 15°–50°, doygunluk >%35) ve neon
(doygunluk >%85, parlaklık >%45) bandını arıyor. Marka çamı `#14615A` (ton ~172°) iki
kurala da girmiyor.

Bugün temiz. Denetimin gerçekten yakaladığı, stile `#ff7a1a` enjekte edilerek doğrulandı.

> Bu bulgu bir ekran görüntüsündeki kırmızımsı kenarlardan şüphelenerek çıktı. Ölçüm
> gösterdi ki o kenarlar **alt piksel yumuşatma artefaktı**, gerçek renk değil. Yani
> göz kararıyla raporlansaydı yanlış bir bulgu olurdu; ölçüm hem bunu engelledi hem de
> kalıcı bir denetim kazandırdı.

---

## Ölçüm araçları yanıldı — iki kez

Prompt *"kanıtla göster"* diyor. Kanıtı üreten aracın kendisi de yanılabilir; bu incelemede
iki kez yanıldı ve ikisi de sahte sonuç üretiyordu.

1. **Tipografi betiği yanlış ögeyi ölçüyordu.** `bolum.querySelector('p')` bölümün
   üstündeki "02 — Sonra hesaplıyoruz" etiketini seçiyordu: tek satır, 23 karakter. Ölçüm
   **her genişlikte aynı sayıyı** veriyordu ve "satır uzunluğu 320'den 3440'a kadar 23
   karakter" gibi anlamsız bir bulgu üretmek üzereydi. *Her genişlikte değişmeyen bir sayı,
   ölçmediğinin işaretidir.*
2. **Önbellek kapatma yöntemi ölçülen şeyi değiştiriyordu.** `Cache-Control: no-cache`
   başlığı Google Fonts isteklerini CORS ön kontrolünde düşürüyor ve sayfa **yedek fontla**
   ölçülüyordu. CDP `Network.setCacheDisabled`'a geçildi ve ölçümler gerçek fontla
   yenilendi.

Ayrıca kenar boşluğu kontrolü ilk hâlinde soldaki 84 pikseli kusur sayıyordu; o boşluk
**cetvel rayının kendisi**, yani markanın ölçü aleti metaforu. Kontrol ray genişliğini
hesaba katacak şekilde düzeltildi. Her koşuda kırmızı gösteren bir araç, kimsenin bakmadığı
bir araç olur.

---

## Kusur bulunmayan alanlar

### Sağlık hesapları — elle ve ızgarayla doğrulandı

Formüller standart ve doğru uygulanmış: Mifflin-St Jeor, Katch-McArdle (`370 + 21.6 × LBM`),
Epley (`kg × (1 + tekrar/30)`, 12 tekrarda sınırlı), Deurenberg, 7700 kcal/kg,
lif 14 g/1000 kcal, su 35 ml/kg.

`tdeeDuzelt` yönü doğru: 14 günde 2000 kcal alıp 1 kg veren kullanıcı için ölçülen TDEE
`2000 − (−1 × 7700 / 14) = 2550`.

Örnek profil yerine **ızgara** sınandı (`packages/core/src/beslenme/supurme.test.ts`):
2 cinsiyet × 5 yaş × 5 boy × 6 kilo × 5 aktivite × 5 hedef = **1.500 profil**.

- Makro toplamı ile beyan edilen kalori arasındaki sapma **≤ 2 kcal** (yuvarlama payı).
- Hiçbir profile **sıfır karbonhidrat** yazılmıyor.
- Kalori mutlak alt sınırın (erkek 1500, kadın 1200) altına inmiyor.

Izgaranın gerçekten döndüğü ve kuralın bozulduğunda kırmızıya döndüğü, eşiğe yapay bir
koşul eklenerek doğrulandı.

### Diğer

- Süre bazlı hareketler program havuzuna girmiyor (yukarıda).
- `set_degisimi` dışında motorda hesaplanıp atılan başka bir değer bulunamadı.
- Tip kaçağı neredeyse yok: tüm depoda **tek** `as unknown as` ve **sıfır** `as any`.

---

## 11-18. İkinci tur: emülatörde 32 ekran, 14 persona, 17 rakip iddiası

İlk tur statik incelemeydi. İkinci turda uygulama derlendi, emülatöre kuruldu ve gerçekten
kullanıldı. **Sekiz kusur daha çıktı; beşi yalnızca cihazda görülebilirdi.**

### 11. Bozuk istek 500 dönüyordu

Hata işleyicisi `UygulamaHatasi`, `ZodError` ve 429'u tanıyordu; geri kalan **her şey**
`500 sunucu_hatasi` oluyordu. Oysa Fastify'ın kendi hataları da `statusCode` taşıyor:

```
DELETE /v1/hesap     content-type json + boş gövde  → 500  (olması gereken 400)
POST  /beslenme/kilo bozuk JSON                     → 500  (400)
POST  /beslenme/kilo content-type xml               → 500  (415)
```

Üç ayrı zarar: kullanıcıya kendi hatası için "sunucu hatası" deniyordu; her bozuk istemci
isteği `beklenmeyen hata` seviyesinde loglanıp gerçek çökmeleri gürültüye gömüyordu; ve
istemci metni `kod` alanından kurduğu için kullanıcı ne yapacağını öğrenemiyordu.

Bu, `cihazda-calistirma.md` 4. maddedeki sınıfın aynısı — orada sunucunun reddi
"bağlantı yok" diye gösteriliyordu.

**Test:** `cerceveHatalari.test.ts`. 4xx geçişi 429'a daraltılınca kırmızıya dönüyor.

### 12. Koyu temada her ana düğme okunmuyordu

`Dugme` birincil metni `'#FFFFFF'` olarak **sabit** yazılmıştı, zemini ise temaya bağlı
aksan rengiydi. Ölçüm:

| Tema | Metin / zemin | Kontrast |
|---|---|---|
| Açık | `#FFFFFF` / `#14615A` | 7.27:1 ✓ |
| Koyu | `#FFFFFF` / `#4FA79C` | **2.86:1** ✗ (AA 4.5, büyük metin 3) |
| Koyu (düzeltilmiş) | `#131614` / `#4FA79C` | 6.37:1 ✓ |

Yani uygulamadaki **her ana düğme** koyu temada eşiğin altındaydı. Aynı hata alışveriş
listesindeki onay tikinde de vardı.

Sitenin koyu teması ölçülüp beş kusur bulunmuştu; **mobil tarafta aynı ölçüm hiç
yapılmamıştı.** `aksanUstu` tokeni eklendi ve `packages/shared/src/kontrast.test.ts`
paleti iki temada ölçüyor. Token geri çevrilince test kırmızıya dönüyor:
`#FFFFFF / #4FA79C: expected 2.86 to be greater than or equal to 3`.

Düzeltme cihazda doğrulandı: "I finished the session, give feedback" düğmesi artık koyu
mürekkeple okunuyor.

### 13. İngilizce arayüzde aynı hareket iki ayrı adla görünüyordu

Cihazda doğrudan görüldü:

```
WHY THIS EXERCISE   Hammer Curl was chosen: ...
(kart başlığı)      Çekiç curl
IF THE MACHINE...   Dumbbell biceps curl · Kabloda biceps curl
```

Gerekçe katmanı `ad_en` kullanıyordu (doğru); arayüzün geri kalanı `ad_tr`'yi koşulsuz
basıyordu — 13 yerde. Kullanıcı bunların aynı hareket olduğunu bilemez.

`shared/hareketAdi.ts` eklendi, 13 kullanım bağlandı, `/program/hareket-degistir` muadil
listesinde artık `ad_en` de dönüyor. Katalogtaki 122 hareketin hepsinde `ad_en` olduğu ve
içinde Türkçe karakter kalmadığı ayrıca sınanıyor.

### 14. İngilizce arayüzde haftalık planda "KAHVALTI" yazıyordu

Öğün adı plan **üretilirken** kullanıcının o anki dilindeki hâliyle `days_jsonb`'ye
yazılıyordu. Dil değiştiğinde kayıt olduğu gibi kalıyordu.

`durum.md` "öğün adları da kapandı" diyordu; kapanan şey **üretim anındaki** dildi, okuma
anındaki değil. Kural yazılıydı ama yalnızca dilini hiç değiştirmeyen kullanıcı için
tutuyordu.

Düzeltme projenin kendi ilkesi: cümleyi saklamak yerine parçalarını saklamak. `kod`
kayıtta duruyor, ad her okumada sözlükten kuruluyor.

### 15. Deste mesajı motorda Türkçe sabitlenmişti

Cihazda İngilizce arayüzde görülen:

> *"Dolabındakilerle bu öğün için seçenek çıkmıyor. zeytinyağı, soğan, haşlanmış nohut
> eklersen 68 seçenek açılıyor."*

`desteHazirla` dört Türkçe cümleyi sabitliyordu. Motor artık kod ve parametre üretiyor,
cümle sözlükte kuruluyor — gerekçe, program uyarısı ve ilerleme kuralında zaten uygulanan
desen. **Malzeme adları Türkçe kalıyor ve bu bilinçli**; sınanan şey cümle.

Test üç öğün × iki dolap durumu = altı kombinasyonu tarıyor.

### 16. İlerleme ekranı ham katalog kimliği basıyordu

Cihazda görüldü — "Hareket bazında ilerleme" bölümü:

```
face-pull         12,5 kg × 10
cekic-curl         8 kg × 15
kablo-gogus-fly   15 kg × 15
```

Bunlar katalog slug'ları. Üstelik ekranın kendi metni bu bölüm için *"gerçek kanıt burada"*
diyor — kanıt okunmuyordu.

### 17-18. İki sessiz hata daha

- **Hesap silme** `void istek(...).then(...)` biçimindeydi: silme patlarsa yakalanmamış bir
  promise reddi kalıyor, ekran değişmiyor ve kullanıcı hesabının silindiğini sanıyordu.
  KVKK bağlamında sessizce tutulmamış bir söz. `hesap_sil` cümlesi eklendi ve bağlandı.
- **Bildirim ekranı** programa ulaşamadığında "Şu an kurulu bildirim yok" diyordu. Doğru
  ama eksik: kullanıcı programı olmadığı için mi, bağlanamadığımız için mi olduğunu
  bilemiyordu.

---

## E — On dört persona, canlı sunucuya karşı

Ondört persona uçtan uca koşturuldu: kaydol → değerlendirme → program → persona başına özel
kontroller. **Hepsi temiz.**

| Persona | Sonuç |
|---|---|
| 52 yaşında, bel fıtığı, evde ekipmansız | Program üretildi, **yerden çekiş yok**, her harekette gerekçe |
| 19 yaşında, hiç salona girmemiş | Program üretildi, gerekçe her harekette |
| 17 yaşında | **Kapı kapandı**, tanı dili yok |
| Gebe | **Kapı kapandı** |
| Kardiyak bayrak | **Kapı kapandı**, tanı dili yok |
| Yeme bozukluğu geçmişi | ED modunda sayılar gizli |
| Vegan + çölyak | **21 öğünün hepsi dolu** |
| Bütçesi kısıtlı, ücretsiz | 1 gün görünür, şapkasız arama çalışıyor |
| Ödeme yapmış | `promosyon_goster=false`, üç yüzeyde upsell yok |
| KVKK'ya duyarlı | Dışa aktarma 16 KB, silme çalışıyor, token geçersizleşiyor |
| Bozuk istek gönderen | Dört bozuk istek, hiçbiri 500 değil, hepsinde kod var |
| Aboneliğini iptal eden | İptal çalışıyor, **hak dönem sonuna kadar korunuyor** |
| Telefon değiştiren | Yeni girişte program yerinde |
| İki hafta sonra dönen | Hafta 1 → 2 ilerledi, **seri/suçluluk dili yok** |

> Kaba kuvvet koruması yolda kendini gösterdi: 11. personada kimlik uçları 429 döndü
> (10 istek/dk). Sınırı testte gevşetmedik — gevşetseydik üretimde olmayan bir şeyi sınamış
> olurduk; personalar arasına yedi saniye kondu.

Bu turda **üç yanlış alarm** doğrulanıp elendi: "hesap silme çalışmıyor" (uç bilerek
`onay: "HESABIMI SİL"` istiyor ve mobil bunu doğru gönderiyor), "2 MB gövde kabul ediliyor"
(`bodyLimit` 12 MB, fotoğraf için bilinçli) ve "evde ekipmansız kullanıcı program alamıyor"
(E3'te "Hiçbiri, vücut ağırlığı" seçeneği var; persona verim yanlıştı).

---

## F — Rakip farkı: on yedi iddia ölçüldü

`scripts/rakip-farki-denetimi.mjs` canlı sunucuya karşı koşuyor. On yedi kontrolün hepsi
geçiyor:

| Rakip şikâyeti | Bizdeki karşılık — ölçüm |
|---|---|
| EatBetter: *"aynı şeyi eklediğimde farklı makrolar"* | İki kayıt birebir aynı |
| EatBetter: *"kaydete basınca reklam çıkıyor"* | Üç yüzeyde upsell metni yok, üç planda da reklam kapalı |
| YAZIO: *"aylık sandım, 600 TL kesilmiş"* (214 beğeni) | Ön seçim yok, yenileme tarihi ve tutar yazılı, aylık/yıllık ayrı |
| YAZIO: *"çocuk muyuz biz"* (55+65 beğeni) | API yüzeylerinde rozet/seri/kutlama dili yok |
| Fitify: *"Türkçe dil desteği"* (144 beğeni) | 122 hareketin hepsinde ≥4 adım Türkçe talimat |
| Fitify: sakatlık farkındalığı (69 beğeni) | Bel fıtığında yerden çekiş yok; elenen 64 hareket söyleniyor |
| Diyetkolik: negatiflerin %34'ü teknik hata | Dört bozuk istek, hiçbiri 500 değil, hepsinde kod var |
| Hevy: ücretsizde bile kullanılabilirlik | 1. gün görülüyor, şapkasız arama çalışıyor |
| **Ürünün ana iddiası** | Sekiz harekette hem gerekçe cümlesi hem ateşlenen kural |
| wger lisans kararı | Katalog kaynağı: free-exercise-db (kamu malı) + exercemus (MIT) |

Denetimin gerçekten yakaladığı, ücretsiz plana bilerek reklam açılıp
`reklamlı plan: ucretsiz` bulgusunun döndüğü görülerek doğrulandı.

Aracın **iki yanlış pozitifi** bulunup düzeltildi: `haklar.reklam: false` alanı "reklam"
geçtiği için ihlal sanılmıştı (oysa tam tersini söylüyor), ve `wger` üç dosyada geçiyordu —
üçünde de wger'in neden **reddedildiği** yazılıydı. Kararı belgeleyen metni kararın ihlali
saymak, aracı güvenilmez yapar.

---

## D — Görsel MCP'ler

Mevcut yapılandırmada `unity-mcp` (bu projeyle ilgisiz), `supabase` ve `vercel` (ikisi de
kimlik doğrulaması bekliyor ve projenin kilitli mimarisiyle — kendi VPS + Postgres —
çelişiyor) vardı.

**Chrome DevTools MCP kuruldu.** Gerekçe: tarayıcı sürme ve ekran görüntüsü için gerçekten
yeni yetenek katan tek aday. **Figma MCP kurulmadı**: projede Figma dosyası yok, marka
varlıkları elle yazılmış SVG — kurulur kurulmaz boşa düşerdi. **Stitch kurulmadı**: promptla
UI üretiyor ve `tokens.ts`, "neon/turuncu yok", "oyunlaştırma yok" kilitli kararlarıyla
çatışır.

`superdesign` ve `frontend-design` zaten skill olarak mevcut; MCP kurulumu gerektirmiyor.

Site tarafındaki asıl iş MCP ile değil, **depoya commit edilen dört betikle** yapıldı —
bunlar CI'da da koşabilir, MCP koşamaz.

---

## C — Emülatörde 32 ekran

Uygulama derlendi (release APK), emülatöre (API 36) kuruldu, gerçek bir Pro hesapla oturum
açıldı ve ekranlar hem açık hem koyu temada, hem Türkçe hem İngilizce gezildi.

Cihazda doğrulanan düzeltmeler:

- **1. bulgu canlı görüldü:** verilen geri bildirim yükü 20 → 22 kg çıkardı ve program
  ekranı *"Single-arm Dumbbell Row 2 × 6-10, 22 kg"* gösterdi. Motor kararı plana yansıyor.
- **4. bulgu canlı görüldü:** akşam destesinin makro kilidi 1079 kcal — günlüğün %40'ı,
  öğlenin %35'i değil.
- **12. bulgu canlı görüldü:** koyu temada ana düğme metni artık okunuyor.
- **13. bulgu canlı görüldü:** kart başlığı, gerekçe ve muadil listesi aynı İngilizce adı
  kullanıyor.
- **7. bulgu iki yönde de doğrulandı:** dil notu Türkçe kullanıcıya gösterilmiyor,
  İngilizce kullanıcıya gösteriliyor.

`scripts/ekran-denetimi.mjs` 32 ekranın tamamını statik olarak da denetliyor: dokunma
hedefi, `accessibilityRole`, durum kapsamı, oyunlaştırma, sabit renk ve ham kimlik. Bugün
temiz.

**Aracın kendi altı yanlış pozitifi bulunup düzeltildi** — hepsi raporlansaydı olmayan
kusurlar bildirilmiş olacaktı:

| Yanlış pozitif | Gerçek |
|---|---|
| `blok-sonu.tsx`: "OYUNLAŞTIRMA: Kutlama" | *"Kutlama yok, konfeti yok"* diyen bir **yorum satırı** |
| 12 ekranda "dokunma hedefi yok" | Regex açılış etiketini `onPress={() =>` içindeki `>` karakterinde kesiyordu |
| `geri-bildirim.tsx` dokunma hedefi | `minHeight` 500 karakterlik pencerenin hemen ötesindeydi |
| `gun.tsx` "boş/hata durumu yok" | Ekranın hem yükleniyor hem "bulunamadı" durumu var |
| `program.tsx` "ham kimlik" | `/v1/program/gerekce/${h.exercise_id}` bir **URL şablonu** |
| `ilerleme.tsx` "ham kimlik" (ikinci) | `key={h.exercise_id}` bir **React anahtarı** |

Cihazda denenmeyenler değişmedi: kamera, gerçek bildirim düşmesi, gerçek satın alma ve iOS.

> **Test için manifest yaması.** Release derlemesi düz HTTP'yi engelliyor (üretimde doğru:
> Caddy HTTPS sunuyor). Yerel API'ye bağlanmak için release manifestine geçici olarak
> `usesCleartextTraffic` eklendi ve **inceleme sonunda geri alındı**. `android/` zaten
> gitignore'da; depoya girmedi.

---

## 19-23. Üçüncü tur: kamera ve bildirimler cihazda

İlk iki turda "cihazda denenmemiş" diye bırakılan iki şey denendi. **Beş kusur daha
çıktı; hiçbiri koddan okunarak bulunamazdı.**

### Kamera — çalışıyor, eğim kapısı ölçüldü

Emülatörün arka kamerası `virtualscene`; canlı görüntü çekim ekranında göründü ve üç poz
(ön, yan, arka) gerçekten çekildi.

**Eğim kapısı cihazda ölçüldü.** `sensor set acceleration` ile ivmeölçer sürüldü:

| Verilen vektör | Ekranda | Deklanşör |
|---|---|---|
| `0:9.81:0` | Tilt 0° | açık |
| `0.5:9.797:0` | Tilt 3° | açık |
| `1.0:9.759:0` | Tilt 6° | açık |
| `1.4:9.71:0` | **Tilt 8°** | **kilitli** |
| `1.7:9.662:0` | Tilt 10° | kilitli |
| `6.94:6.94:0` | Tilt 45° | kilitli |

`EGIM_TOLERANSI = 8` ile birebir. Kilitliyken düğme görsel olarak da pasif ve uyarı
amber'a dönüyor: *"Straighten the phone. Capture stays locked until the angle is right."*

Bu, `cihazda-calistirma.md` 13. maddede düzeltilen hatanın (Android'de işaret ters,
kapı hiç açılmıyordu) gerçek sensörle doğrulanması.

**Fotoğraf diske yazılmıyor.** Üç çekimden sonra `/sdcard` üzerinde son 30 dakikada
değişen tek bir görüntü dosyası yok; medya veritabanına yeni kayıt girmedi. Sunucu
tarafında `body_analyses` şemasında fotoğraf sütunu zaten yok ve API logunda
`base64|data:image` deseni **sıfır** kez geçiyor.

### 19. Ücretsiz kullanıcı kendi vücut analizini hiç göremiyordu

**En ağır bulgu.** Kullanıcı "Analizi başlat"a **bir kez** basıyor, sunucuya **iki**
istek gidiyor (76 ms arayla). Tek düğme değil, iki ekran aynı ucu çağırıyor:

```
fotograf/cekim.tsx  → POST /vucut/analiz (fotoğraflarla) → 200, hak harcanır
rapor/index.tsx     → POST /vucut/analiz (boş gövdeyle)  → 403, hak bitti
```

`POST /analiz` bilerek yaratıcı bir uç: her çağrıda yeni analiz üretir. Sorun ucun
kendisi değil, **raporu okumak için de onun çağrılmasıydı.**

Ücretsiz katmanda sonuç yıkıcı: ömür boyu tek analiz hakkı kullanıcının kendi çekimiyle
harcanıyor, sonra rapor ekranı aynı ucu tekrar çağırıp 403 alıyor ve kullanıcı **kendi
analizini hiç göremiyor.** `CLAUDE.md`'nin ücretsiz kapsam satırındaki *"Vücut analizi
(bir kez)"* — teslim edilen tek çıktı — normal akışta hiç görünmüyordu.

Düzeltme: okuma ile yazma ayrıldı (`GET /vucut/analiz/son`, hak harcamaz). Cihazda
doğrulandı: tek dokunuşta **1 POST** ve rapor görüntüleniyor.

### 20-21. Düzeltmenin kendisi iki kusur daha üretti — ikisi de cihazda yakalandı

Yeni okuma ucu ilk hâlinde:

1. **Çeviriyi yarım bırakıyordu.** Yalnızca `ozet` ve `durus` çeviriliyor; `sinirlamalar`,
   `feragat` ve bel/boy mesajı Türkçe kalıyordu. İngilizce arayüzde doğrudan görüldü ve
   kaçaklardan biri **tıbbi cihaz feragatiydi** — `durum.md`'nin *"çeviride kaybolabilecek
   en tehlikeli cümle"* dediği cümle.

2. **Gizlilik notunu çıkarımla seçiyordu.** Duruş bayrağı sayısına bakıyordu; görsel analiz
   bayrak üretmediğinde (AI geçidi yapılandırılmamışsa) üç fotoğraf çeken kullanıcıya
   *"fotoğraf göndermedin"* diyordu. Ürünün en hassas cümlesi tahmin edilecek yer değil:
   göç `0006` ile kalıcı alan eklendi.

> Alan adı **iki kez** değişti ve ikisini de mevcut gizlilik korumaları zorladı:
> `fotografli` sütunu `sema.test.ts`'i, `fotografGeldi` değişkeni `gizlilik.test.ts`'i
> kırmızıya düşürdü. İkisi de haklı — saklanan şey fotoğraf değil, raporun hangi
> kaynaktan üretildiği. `gorselden_uretildi` oldu.

### 22-23. Bildirimler gerçekten düşüyor — ama uygulama açıkken kayboluyordu

Alarm saati `cmd alarm set-time` ile ileri alınarak denendi.

**Düştüğü görüldü.** Uygulama arka plandayken bildirim gölgeye girdi:

```
NotificationRecord(pkg=io.made2fit.app tag=geri_bildirim-5-19-30 importance=4
  color=0xff14615a)
  android.title = "How did the session go?"
  android.text  = "Three taps: effort, pain, duration. The next session is
                   calculated from them."
```

Marka ikonu, marka çamı, kullanıcının dilinde, suçluluk dili yok, seri yok.

**22. Uygulama ön plandayken bildirim sessizce kayboluyordu.** 18:00 alarmı uygulama
açıkken tetiklendi (alarm sistem listesinden düştü) ama gölgeye hiç girmedi.
`expo-notifications` sözleşmesi: ön planda gelen bildirim işleyiciye sorulur ve işleyici
yoksa varsayılan "gösterme"dir. `setNotificationHandler` kod tabanının **hiçbir yerinde**
tanımlı değildi — davranış seçilmemiş, kütüphane varsayılanına bırakılmıştı.

İşleyici eklendi ve **kök düzenden** yükleniyor; yalnızca zamanlayıcı modülünde bırakmak
yetmezdi, çünkü o modül bildirim ayarları ekranından yükleniyor ve o ekrana hiç girmeyen
kullanıcıda işleyici hiç kurulmazdı.

> **Kendi düzeltmem ilk hâlinde yanlıştı ve bunu da cihaz yakaladı.** Handler'a
> `shouldShowBanner`/`shouldShowList` yazdım; kurulu `expo-notifications@0.29.14`
> **`shouldShowAlert`** bekliyor (o adlar daha yeni SDK'ların). TypeScript uyarmadı,
> cihaz uyardı. Deponun tetikleyici biçimi için yazdığı kural burada da geçerliydi:
> *biçim tahminle değil, kurulu paketin tipinden yazılır.*
>
> Aynı okuma ikinci bir şey daha gösterdi: kütüphanenin kendi notuna göre Android'de
> `shouldPlaySound: false` **uyarıyı da gizliyor**. Yani "sessiz olsun" demek bildirimi
> tamamen kaybetmek olurdu; sessizlik kanal seviyesine alındı — hem doğru yer, hem
> kullanıcının sistem ayarlarından değiştirebileceği yer.

**23. Beş hatırlatma türü tek bir yedek kanaldaydı.** Bildirim
`expo_notifications_fallback_notification_channel` ile düşüyordu: uygulama hiç kanal
tanımlamamış. Android 8'den beri kanal kullanıcıya görünür; sistem ayarlarında beş tür
tek bir genel başlık altında toplanıyor ve kullanıcı yalnızca su hatırlatmasını
susturamıyordu. Uygulama içi anahtarlar var ama bildirim ayarına telefondan bakan
kullanıcı azınlık değil.

Her türe kendi kanalı verildi. Cihazda doğrulandı:

```
made2fit-seans  made2fit-geri_bildirim  made2fit-haftalik_ozet
made2fit-olcum  made2fit-su
```

> **Dürüst not — ön plan düzeltmesi cihazda yeniden doğrulanamadı.** Tekrarlanan saat
> oynatmalarından sonra emülatörün AlarmManager'ı bu alarmları güvenilir biçimde teslim
> etmeyi bıraktı (yeniden başlatma da açmadı): sonraki denemelerde alarm hiç tetiklenmedi,
> yani test **sonuçsuz** kaldı, olumsuz değil. Kanal düzeltmesi cihazda doğrulandı;
> işleyici düzeltmesi kütüphanenin belgelenmiş sözleşmesine ve kurulu paketin tipine
> dayanıyor, ayrıca `zamanlayici.test.ts` ile sabitlendi — ama "cihazda ön planda düştüğü
> görüldü" diyemem.

---

## Yapılmayanlar ve neden

Promptun yedi başlığının (A–G) tamamı yapıldı. Geriye kalanlar, bu ortamda **yapılamayacak**
olanlar:

- **Barkod kamerası.** Vücut fotoğrafı akışı çekim dahil denendi (bölüm 19-21); barkod
  ekranının kamera yolu denenmedi, elle giriş yoluyla sınandı.
- **Ön planda bildirim düşmesi.** Bildirimin gerçekten düştüğü görüldü (bölüm 22), ama
  ön plan düzeltmesi cihazda yeniden doğrulanamadı: emülatörün AlarmManager'ı tekrarlanan
  saat oynatmalarından sonra alarmları teslim etmeyi bıraktı. Test sonuçsuz kaldı,
  olumsuz değil.
- **Gerçek satın alma.** RevenueCat hesabı ve mağaza ürünleri yok. Paywall açılıyor,
  fiyatlar ve yenileme tarihi görünüyor, satın alma denenmedi.
- **iOS.** Yalnızca Android'de çalıştırıldı.
- **Play Console.** `info@swiip.app` Google oturumu yok ve giriş iki adım doğrulama
  gerektiriyor — promptun kendisi bunu zaten dışarıda bırakıyor.

Bunlar "sonra bakılacak" listesi değil; **bu ortamda yapılamayacağı gerekçesiyle**
yapılmayan işler. Hepsi `durum.md`'de de aynı gerekçeyle yazılı.

---

## Değişen dosyalar

| Commit | Konu |
|---|---|
| `381f474` | İlerleme döngüsü + sonraki hafta + beslenme ızgarası |
| `f2b0fe4` | Öğün değiştirme + öğün kodu eşleşmesi + sessiz yazma hataları |
| `be2fa8f` | Dil notu, önbellek yaşı, iki ölü yardımcı |
| `2d9c948` | Yetki açığı + kiracı ayrımı testleri |
| `b188dc0` | Site tipografi ölçeği + üç denetim betiği |
| `63d448f` | Denetim betikleri lint kapsamına alındı |
| `9fb65b6` | Rapor + `durum.md`'de yanlış çıkan iki iddianın düzeltilmesi |
| `66442b6` | Bozuk isteklerin 500 dönmesi + hesap silme reddi |
| `2d093af` | Koyu tema düğme kontrastı + İngilizce hareket adları + ekran denetimi |
| `8fd839a` | Ekran denetimi aracının gürültüsü + dokunma hedefi garantileri |
| `5f8f6ff` | Rakip farkı denetimi (17 iddia) |
| `fa46eef` | Plandaki öğün adının okuma anında çözülmesi |
| `1db837d` | Deste mesajının sözlükten kurulması |
| `481d3d8` | İlerleme ekranındaki ham katalog kimliği |

Yeni denetim komutları:

```
# Site (tarayıcı ister)
npx serve -l 8090 -s apps/site                  # başka bir kabukta
node scripts/site-kontrast.mjs                  # kontrast, iki tema, üç sayfa
node scripts/site-gorsel.mjs <adres> <klasör>   # 168 görüntü + taşma + kumpas
node scripts/site-tipografi.mjs                 # ölçek, satır uzunluğu, sağ boşluk
node scripts/site-renk-denetimi.mjs             # neon ve turuncu yok

# Mobil ve ürün (tarayıcı istemez)
node scripts/ekran-denetimi.mjs                 # 32 ekran: dokunma hedefi, erişilebilirlik,
                                                # durum kapsamı, oyunlaştırma, ham kimlik
node scripts/rakip-farki-denetimi.mjs           # 17 rakip iddiası, canlı API'ye karşı
```

Emülatörde kamera ve bildirim denemesi için:

```
adb emu sensor set acceleration 0:9.81:0        # telefon dik; eğim kapısı açılır
adb emu sensor set acceleration 6.94:6.94:0     # 45° eğim; deklanşör kilitlenir
adb shell cmd alarm set-time <epoch_ms>         # alarm saatini ileri al
adb shell dumpsys alarm | grep made2fit         # kurulu hatırlatmalar
adb shell dumpsys notification --noredact       # düşen bildirim
```

> Saati **ileri** almak gerekiyor ve tek seferde: geri alıp tekrar ileri almak
> emülatörün alarm defterini bozuyor ve alarmlar bir daha teslim edilmiyor.

`rakip-farki-denetimi.mjs` çalışan bir API ister:

```
docker compose -f infra/docker-compose.yml -f infra/docker-compose.gelistirme.yml up -d
PORT=3311 npm -w @made2fit/api run dev
```

---

## Bu incelemenin kendi dersi

Yirmi üç bulgunun yanında, **ölçüm araçlarımda ve kendi düzeltmelerimde on beş hata**
bulundu ve düzeltildi. Hepsi rapor edilseydi olmayan kusurlar bildirilmiş olacaktı:

| Araç | Hata | Nasıl anlaşıldı |
|---|---|---|
| Ölü kod taraması | Sembolün kendi dosyasındaki kullanımı sayılmıyordu | 535 exportun 535'i "ölü" çıktı |
| Ölü kod taraması | drizzle `relations` şemaya veriliyor, ölü değil | Bağlantı dosyası okundu |
| Tipografi ölçümü | Bölümün ilk `<p>`'sini (etiket) ölçüyordu | Her genişlikte aynı sayı çıktı |
| Tipografi ölçümü | `Cache-Control` başlığı Google Fonts'u CORS'ta düşürüyordu | Konsol hataları görüldü |
| Görsel tarama | Soldaki 84 px'i kusur sayıyordu; o cetvel rayı | Ekran görüntüsüne bakıldı |
| Ekran denetimi | `onPress={() =>` içindeki `>` regex'i kesiyordu | 12 ekranda "hedef yok", kodda `minHeight` vardı |
| Ekran denetimi | Yorumları da tarıyordu | "Kutlama yok" yorumu ihlal sayıldı |
| Ekran denetimi | `${...}` ve `key={...}` metin sanılıyordu | URL ve React anahtarı |
| Rakip denetimi | `haklar.reklam: false` alanı ihlal sanıldı | Alan tam tersini söylüyor |
| Rakip denetimi | `wger` geçen dosyalar ihlal sanıldı | Üçü de reddedilme gerekçesi |
| Persona betiği | Uydurulmuş şık değerleri (`"Ekipmanım yok"`) | 400 dönen kayıt izlendi |
| Kabuk | Türkçe karakterleri bozuyordu (`Bel fıtığı` → `Bel fitigi`) | Sunucu "tanımadığım seçenek" dedi |
| Bildirim düzeltmem | `shouldShowBanner`/`shouldShowList` — kurulu paket `shouldShowAlert` bekliyor | Cihazda bildirim yine düşmedi |
| Rapor okuma ucum | Çeviri alanlarının yarısını düşürüyordu | İngilizce ekranda Türkçe feragat görüldü |
| Rapor okuma ucum | Gizlilik notunu duruş bayrağından çıkarıyordu | "Fotoğraf göndermedin" dedi, üç fotoğraf gönderilmişti |

Bunun bir sonucu var: **"araç bulgu verdi" bir bulgu değildir.** Her biri kaynağa bakılarak
doğrulandı ve doğrulanmayan hiçbiri rapora girmedi. Aynı disiplinin tersi de uygulandı —
her düzeltme, korumayı bilerek bozup kırmızıya döndüğü görülerek kapatıldı.
